export function getListingLink(id: string): string {
  return `https://www.seekapp.uk/link/location/${id}/details`;
}

export function getApplicationLink(id: string): string {
  return `https://www.seekapp.uk/link/chat/${id}/join`;
}
